// syntax for one parameter one parameter and single return statememt
let square = (x) => x*x; // => denotes return.
// console.log(square(6));

// syntax for multiple parametes and one single return statement.

let sum = (x,y,z) => x+y+z;
// console.log(sum(5,6,7));


